# Test profile for rate-fx (activated with @ActiveProfiles("test")).
#
# T0-2: the service now refuses to boot unless the internal-auth gate over
# POST /v1/rates/snapshots is armed with a non-blank secret (see
# com.gme.pay.ratefx.config.InternalAuthEnforcedConfig), so any test that starts the full
# application context has to supply one. This value is a TEST FIXTURE, not a credential: it lives in
# the test source set, is never packaged into the service jar, and must stay in sync with
# com.gme.pay.ratefx.testsupport.TestInternalAuth#SECRET.
gmepay.internal-auth.secret=test-fixture-internal-token-not-a-deployment-secret
