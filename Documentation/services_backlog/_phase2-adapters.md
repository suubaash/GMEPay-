# Phase-2+ scheme adapters — service backlog bundle

Post-go-live adapters (KHQR, QPay, QRIS, SBP, PromptPay). Placeholders only — expand each into a WS9-shaped ticket set when the partner contract lands.


<!-- wbs-v3-gap-closure -->

---

## WBS v3 gap-closure tickets (re-baseline, 2026-06-10)

These tickets convert this service's PARTIAL audit findings into DONE and add work discovered during the build. Statuses live on the `Backlog` sheet of `GMEPay+_Task_Backlog.xlsx`; phase sequencing on the `Completion Plan v3` sheet of `GMEPay+_WBS.xlsx`.

### 20.1-G01 — Expand WBS for KHQR (Cambodia) adapter on contract signing
*Completion phase:* **R8** · *Est:* 240 min · *Role:* Architect · *Deps:* 9.1-T01,16.2

**Context.** Post-go-live Phase 2+ scheme. Placeholder: when the partner contract + scheme spec land, expand into a full work-package set mirroring WS9 (abstraction already supports pluggable adapters via 9.1).

**Steps.**
- Obtain scheme spec + sandbox creds
- Clone WS9 ticket structure, adjust to scheme protocol (API vs file)
- Estimate + schedule

**Deliverable.** Expanded WBS for KHQR (Cambodia)

**Acceptance.**
- Ticket set reviewed; adapter implements SchemeAdapter interface from 9.1

### 20.2-G01 — Expand WBS for QPay (Mongolia) adapter on contract signing
*Completion phase:* **R8** · *Est:* 240 min · *Role:* Architect · *Deps:* 9.1-T01,16.2

**Context.** Post-go-live Phase 2+ scheme. Placeholder: when the partner contract + scheme spec land, expand into a full work-package set mirroring WS9 (abstraction already supports pluggable adapters via 9.1).

**Steps.**
- Obtain scheme spec + sandbox creds
- Clone WS9 ticket structure, adjust to scheme protocol (API vs file)
- Estimate + schedule

**Deliverable.** Expanded WBS for QPay (Mongolia)

**Acceptance.**
- Ticket set reviewed; adapter implements SchemeAdapter interface from 9.1

### 20.3-G01 — Expand WBS for QRIS (Indonesia) adapter on contract signing
*Completion phase:* **R8** · *Est:* 240 min · *Role:* Architect · *Deps:* 9.1-T01,16.2

**Context.** Post-go-live Phase 2+ scheme. Placeholder: when the partner contract + scheme spec land, expand into a full work-package set mirroring WS9 (abstraction already supports pluggable adapters via 9.1).

**Steps.**
- Obtain scheme spec + sandbox creds
- Clone WS9 ticket structure, adjust to scheme protocol (API vs file)
- Estimate + schedule

**Deliverable.** Expanded WBS for QRIS (Indonesia)

**Acceptance.**
- Ticket set reviewed; adapter implements SchemeAdapter interface from 9.1

### 20.4-G01 — Expand WBS for SBP (Russia) adapter on contract signing
*Completion phase:* **R8** · *Est:* 240 min · *Role:* Architect · *Deps:* 9.1-T01,16.2

**Context.** Post-go-live Phase 2+ scheme. Placeholder: when the partner contract + scheme spec land, expand into a full work-package set mirroring WS9 (abstraction already supports pluggable adapters via 9.1).

**Steps.**
- Obtain scheme spec + sandbox creds
- Clone WS9 ticket structure, adjust to scheme protocol (API vs file)
- Estimate + schedule

**Deliverable.** Expanded WBS for SBP (Russia)

**Acceptance.**
- Ticket set reviewed; adapter implements SchemeAdapter interface from 9.1

### 20.5-G01 — Expand WBS for PromptPay (Thailand) adapter on contract signing
*Completion phase:* **R8** · *Est:* 240 min · *Role:* Architect · *Deps:* 9.1-T01,16.2

**Context.** Post-go-live Phase 2+ scheme. Placeholder: when the partner contract + scheme spec land, expand into a full work-package set mirroring WS9 (abstraction already supports pluggable adapters via 9.1).

**Steps.**
- Obtain scheme spec + sandbox creds
- Clone WS9 ticket structure, adjust to scheme protocol (API vs file)
- Estimate + schedule

**Deliverable.** Expanded WBS for PromptPay (Thailand)

**Acceptance.**
- Ticket set reviewed; adapter implements SchemeAdapter interface from 9.1

