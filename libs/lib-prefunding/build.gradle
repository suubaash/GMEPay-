// lib-prefunding: prepaid USD balance + deduction logic (atomic at the DB layer in the service).
dependencies {
    implementation project(':libs:lib-errors')
    implementation project(':libs:lib-money')
}
